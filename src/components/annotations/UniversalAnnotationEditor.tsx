/**
 * Jednotný editor anotací — layout a dialog viz {@link UnifiedAnnotationEditor}.
 * Obchodní logika a stav zůstávají v `JobDetailPageContent` (otevření přes `onAnnotatePhoto` / měření).
 */
export type {
  UniversalAnnotationRecord,
  UniversalAnnotationRecordType,
} from "./universal-annotation-record";

export { UnifiedAnnotationEditor } from "./UnifiedAnnotationEditor";
